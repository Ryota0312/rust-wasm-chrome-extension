## rust-wasm-chrome-extension-example

1. Build

    ```
    $ yarn build
    ```

2. Load as chrome extension

    - Chromeで`chrome://extensions`にアクセス
    - デベロッパーモードを有効化
    - 「パッケージ化されてない拡張機能を読み込む」で`./dist`を読み込み